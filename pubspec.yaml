name: emo_pdf_reader
description: PDF-to-voice reader (pure Dart PDF)
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_tts: ^3.8.3
  file_picker: ^6.1.1
  syncfusion_flutter_pdf: ^24.1.41

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
